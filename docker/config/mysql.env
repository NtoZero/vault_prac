# ─── MySQL 인증 정보 ─────────────────────────────────────────
# root 계정 비밀번호 (필수)
MYSQL_ROOT_PASSWORD=ChangeMeRoot!

# 최초 컨테이너 기동 시 자동으로 생성할 데이터베이스 이름 (선택)
MYSQL_DATABASE=demo

# 최초 컨테이너 기동 시 자동으로 생성할 일반 사용자 이름 (선택)
MYSQL_USER=app_user

# 위 사용자 비밀번호 (MYSQL_USER가 지정된 경우 필수)
MYSQL_PASSWORD=app_pass_ChangeMe!

# ─── 타임존 설정 (선택) ───────────────────────────────────────
# 컨테이너 내부 시간대를 Asia/Seoul로 맞춥니다.
TZ=Asia/Seoul